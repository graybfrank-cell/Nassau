"use client";

// The Captain's Command Center is the hero marketing shot of the product.
// We render the exact same JSX that /demo/planning renders so there is no
// UI drift between the demo and the capture route.
import DemoPlanningPage from "@/app/demo/planning/page";

export default function CommandCenterScreenshot() {
  return <DemoPlanningPage />;
}
